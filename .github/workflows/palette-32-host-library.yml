name: Palette 32 host and Library integration

on:
  push:
    branches:
      - palette/32-variants-host-library

permissions:
  contents: write

jobs:
  integrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: palette/32-variants-host-library
          fetch-depth: 0
      - name: Apply variant host and Library slice
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace(path, old, new, count=1):
              p = Path(path)
              s = p.read_text()
              actual = s.count(old)
              if actual < count:
                  raise SystemExit(f"{path}: expected {count} occurrence(s), found {actual}: {old[:100]!r}")
              p.write_text(s.replace(old, new, count))

          def insert_before(path, marker, text):
              replace(path, marker, text + marker)

          # ---- Shared scene+variant selection in Library persistence ----
          store = "Sources/Harness/SceneLibraryStore.swift"
          replace(store, "    static let catalogVersion = 2\n", "    static let catalogVersion = 3\n")
          replace(store, '''    struct Collection: Codable, Equatable {
        var id: String = UUID().uuidString
        var name: String
        var sceneIDs: [String] = []
        var playback: Playback?
    }
''', '''    struct SceneSelection: Codable, Equatable, Hashable, Sendable {
        var sceneID: String
        var variantID: UUID? = nil
    }
    struct Collection: Codable, Equatable {
        var id: String
        var name: String
        var selections: [SceneSelection]
        var playback: Playback?
        var sceneIDs: [String] {
            get { selections.map(\\.sceneID) }
            set { selections = newValue.map { SceneSelection(sceneID: $0) } }
        }
        func selection(for sceneID: String) -> SceneSelection? {
            selections.first { $0.sceneID == sceneID }
        }
        init(id: String = UUID().uuidString, name: String, sceneIDs: [String] = [],
             selections: [SceneSelection]? = nil, playback: Playback? = nil) {
            self.id = id
            self.name = name
            self.selections = selections ?? sceneIDs.map { SceneSelection(sceneID: $0) }
            self.playback = playback
        }
        enum CodingKeys: String, CodingKey { case id, name, selections, sceneIDs, playback }
        init(from decoder: Decoder) throws {
            let values = try decoder.container(keyedBy: CodingKeys.self)
            id = try values.decodeIfPresent(String.self, forKey: .id) ?? UUID().uuidString
            name = try values.decode(String.self, forKey: .name)
            playback = try values.decodeIfPresent(Playback.self, forKey: .playback)
            if let stored = try values.decodeIfPresent([SceneSelection].self, forKey: .selections) {
                selections = stored
            } else {
                selections = try values.decodeIfPresent([String].self, forKey: .sceneIDs)?.map { SceneSelection(sceneID: $0) } ?? []
            }
        }
        func encode(to encoder: Encoder) throws {
            var values = encoder.container(keyedBy: CodingKeys.self)
            try values.encode(id, forKey: .id)
            try values.encode(name, forKey: .name)
            if !selections.isEmpty { try values.encode(selections, forKey: .selections) }
            try values.encodeIfPresent(playback, forKey: .playback)
        }
    }
''')
          replace(store, '''        for i in next.collections.indices { next.collections[i].sceneIDs.removeAll { $0 == id } }
''', '''        for i in next.collections.indices { next.collections[i].selections.removeAll { $0.sceneID == id } }
''')
          replace(store, '''    func toggleMembership(sceneID: String, collectionID: String) throws {
        var next = catalog
        guard let index = next.collections.firstIndex(where: { $0.id == collectionID }) else { throw failure("Collection no longer exists.") }
        if next.collections[index].sceneIDs.contains(sceneID) {
            next.collections[index].sceneIDs.removeAll { $0 == sceneID }
        } else { next.collections[index].sceneIDs.append(sceneID) }
        try save(next)
    }
''', '''    func toggleMembership(sceneID: String, collectionID: String) throws {
        try toggleMembership(selection: SceneSelection(sceneID: sceneID), collectionID: collectionID)
    }
    func toggleMembership(selection: SceneSelection, collectionID: String) throws {
        var next = catalog
        guard let index = next.collections.firstIndex(where: { $0.id == collectionID }) else { throw failure("Collection no longer exists.") }
        if let existing = next.collections[index].selections.firstIndex(where: { $0.sceneID == selection.sceneID }) {
            if next.collections[index].selections[existing] == selection {
                next.collections[index].selections.remove(at: existing)
            } else {
                next.collections[index].selections[existing] = selection
            }
        } else {
            next.collections[index].selections.append(selection)
        }
        try save(next)
    }
''')
          replace(store, '''    func moveScene(_ sceneID: String, in collectionID: String, by offset: Int) throws {
        var next = catalog
        guard let collection = next.collections.firstIndex(where: { $0.id == collectionID }),
              let index = next.collections[collection].sceneIDs.firstIndex(of: sceneID),
              [-1, 1].contains(offset) else { throw failure("Select a scene in a collection.") }
        let destination = index + offset
        guard next.collections[collection].sceneIDs.indices.contains(destination) else { return }
        next.collections[collection].sceneIDs.swapAt(index, destination)
        try save(next)
    }
''', '''    func moveScene(_ sceneID: String, in collectionID: String, by offset: Int) throws {
        var next = catalog
        guard let collection = next.collections.firstIndex(where: { $0.id == collectionID }),
              let index = next.collections[collection].selections.firstIndex(where: { $0.sceneID == sceneID }),
              [-1, 1].contains(offset) else { throw failure("Select a scene in a collection.") }
        let destination = index + offset
        guard next.collections[collection].selections.indices.contains(destination) else { return }
        next.collections[collection].selections.swapAt(index, destination)
        try save(next)
    }
''')
          replace(store, '''              value.collections.allSatisfy({ !$0.name.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty && $0.name.utf8.count <= 120 && $0.id.utf8.count <= 128 && $0.sceneIDs.count <= 256 && Set($0.sceneIDs).count == $0.sceneIDs.count && $0.sceneIDs.allSatisfy { $0.utf8.count <= 128 } })
''', '''              value.collections.allSatisfy({ !$0.name.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty && $0.name.utf8.count <= 120 && $0.id.utf8.count <= 128 && $0.selections.count <= 256 && Set($0.selections.map(\\.sceneID)).count == $0.selections.count && $0.selections.allSatisfy { !$0.sceneID.isEmpty && $0.sceneID.utf8.count <= 128 } })
''')

          # ---- Wallpaper canonical source + persisted variant + temporary runtime controls ----
          wall = "Sources/Wallpaper/WallpaperController.swift"
          replace(wall, '''    private(set) var pausedByUser = false
    private var playable: SceneDescriptor?
''', '''    private(set) var pausedByUser = false
    private var playable: SceneDescriptor?
    private var sourceScene: SceneDescriptor?
    private(set) var activeVariantID: UUID?
    private(set) var unavailableVariantID: UUID?
    private var runtimeControlValues: [String: SceneControlValue] = [:]
    private var variantWarning: String?
''')
          replace(wall, '''    private static let pauseKey = "wallpaperResumePaused"
''', '''    private static let pauseKey = "wallpaperResumePaused"
    private static let resumeVariantKey = "wallpaperResumeVariantID"
''')
          replace(wall, '''            select(url, automatic: true)
''', '''            let variantID = resumeDefaults.string(forKey: Self.resumeVariantKey).flatMap(UUID.init(uuidString:))
            select(url, variantID: variantID, automatic: true)
''', 1)
          replace(wall, '''            resumeDefaults.set(pausedByUser, forKey: Self.pauseKey)
''', '''            resumeDefaults.set(pausedByUser, forKey: Self.pauseKey)
            if let activeVariantID { resumeDefaults.set(activeVariantID.uuidString, forKey: Self.resumeVariantKey) }
            else { resumeDefaults.removeObject(forKey: Self.resumeVariantKey) }
''', 1)
          replace(wall, '''    var onManualSelection: (() -> Void)?
    /// Fires after any successful (non-reload, non-transient) selection with the adopted URL.
    var onSelectionCommitted: ((URL) -> Void)?
''', '''    var onManualSelection: (() -> Void)?
    /// Fires after any successful (non-reload, non-transient) selection with the adopted URL.
    var onSelectionCommitted: ((URL) -> Void)?
    var onSelectionCommittedVariant: ((URL, UUID?) -> Void)?
''')
          replace(wall, '''    private var prePeekURL: URL?
    /// A peek is a transient preview: no resume-bookmark save, no day-scene
    /// adoption, no rotation interference. The previous scene is restored on exit.
    var isPeeking: Bool { prePeekURL != nil }
    func peek(_ url: URL) {
        if prePeekURL == nil { prePeekURL = selectedURL }
        guard url != selectedURL else { return }
        select(url, automatic: true, restoringPause: pausedByUser, transient: true)
    }
    func endPeek(reverting: Bool = true) {
        guard let back = prePeekURL else { return }
        prePeekURL = nil
        guard reverting, back != selectedURL else { return }
        select(back, automatic: true, restoringPause: pausedByUser, transient: true)
    }
''', '''    private var prePeekSelection: (url: URL, variantID: UUID?)?
    /// A peek is a transient preview: no resume-bookmark save, no day-scene
    /// adoption, no rotation interference. The previous scene is restored on exit.
    var isPeeking: Bool { prePeekSelection != nil }
    func peek(_ url: URL) {
        if prePeekSelection == nil, let selectedURL { prePeekSelection = (selectedURL, activeVariantID) }
        guard url != selectedURL else { return }
        select(url, variantID: nil, automatic: true, restoringPause: pausedByUser, transient: true)
    }
    func endPeek(reverting: Bool = true) {
        guard let back = prePeekSelection else { return }
        prePeekSelection = nil
        guard reverting, back.url != selectedURL || back.variantID != activeVariantID else { return }
        select(back.url, variantID: back.variantID, automatic: true, restoringPause: pausedByUser, transient: true)
    }
''')
          insert_before(wall, '''    func select(_ url: URL, reloading: Bool = false, automatic: Bool = false, restoringPause: Bool? = nil, transient: Bool = false) {
''', '''    private struct EffectiveScene {
        var scene: SceneDescriptor
        var selectedVariantID: UUID?
        var unavailableVariantID: UUID?
        var runtimeValues: [String: SceneControlValue]
    }
    private func compose(source: SceneDescriptor, variantID: UUID?, runtimeValues: [String: SceneControlValue]) -> EffectiveScene {
        let application = source.applyingVariant(id: variantID)
        var scene = application.scene
        var compatible: [String: SceneControlValue] = [:]
        for (key, value) in runtimeValues {
            guard let parameter = scene.parameters[key], let replacement = value.applying(to: parameter) else { continue }
            scene.parameters[key] = replacement
            compatible[key] = value
        }
        return EffectiveScene(scene: scene, selectedVariantID: application.selectedVariantID,
            unavailableVariantID: variantID != nil && application.selectedVariantID == nil ? variantID : nil,
            runtimeValues: compatible)
    }
    private func updateLiveScene(_ next: SceneDescriptor, rollingBackTo previous: SceneDescriptor?) throws {
        _ = try next.evaluated()
        for surface in surfaces {
            guard surface.updateScene(next) else {
                if let previous { surfaces.forEach { _ = $0.updateScene(previous) } }
                throw SceneError.invalid("The scene controls could not be applied. The previous values were restored.")
            }
        }
    }
    private func applyVariantSelection(_ id: UUID?, persist: Bool) throws {
        guard let sourceScene, let selectedURL, !isLoading else {
            throw SceneError.invalid("Select an active scene before choosing a variant.")
        }
        let previous = playable
        let effective = compose(source: sourceScene, variantID: id, runtimeValues: [:])
        try updateLiveScene(effective.scene, rollingBackTo: previous)
        playable = effective.scene
        activeVariantID = effective.selectedVariantID
        unavailableVariantID = effective.unavailableVariantID
        runtimeControlValues = [:]
        variantWarning = effective.unavailableVariantID == nil ? nil : "Saved variant is unavailable; using Default."
        syncSystemBackdrop(scene: effective.scene, sourceURL: selectedURL, request: generation)
        if persist { saveSelection() }
        updateMenu()
    }
    func setActiveVariant(_ id: UUID?) throws { try applyVariantSelection(id, persist: true) }

''')
          replace(wall, '''    func select(_ url: URL, reloading: Bool = false, automatic: Bool = false, restoringPause: Bool? = nil, transient: Bool = false) {
''', '''    func select(_ url: URL, variantID: UUID? = nil, reloading: Bool = false, automatic: Bool = false, restoringPause: Bool? = nil, transient: Bool = false) {
        if !reloading, !isLoading, let selectedURL, sourceScene != nil,
           selectedURL.standardizedFileURL == url.standardizedFileURL {
            if !automatic { onManualSelection?() }
            do {
                try applyVariantSelection(variantID, persist: !transient)
                if !transient {
                    onSelectionCommitted?(url)
                    onSelectionCommittedVariant?(url, activeVariantID)
                }
                onStart?()
            } catch { showError(error.localizedDescription) }
            return
        }
''')
          replace(wall, '''                let playable = try await self.source.resolve(url)
                for node in playable.allNodes where node.kind == .video {
''', '''                let sourceScene = try await self.source.resolve(url)
                let requestedVariantID = reloading ? (variantID ?? self.activeVariantID) : variantID
                let retainedRuntime = reloading ? self.runtimeControlValues : [:]
                let effective = self.compose(source: sourceScene, variantID: requestedVariantID, runtimeValues: retainedRuntime)
                let playable = effective.scene
                for node in playable.allNodes where node.kind == .video {
''')
          replace(wall, '''                self.playable = playable
                adopted = true
''', '''                self.playable = playable
                self.sourceScene = sourceScene
                self.activeVariantID = effective.selectedVariantID
                self.unavailableVariantID = effective.unavailableVariantID
                self.runtimeControlValues = effective.runtimeValues
                self.variantWarning = effective.unavailableVariantID == nil ? nil : "Saved variant is unavailable; using Default."
                adopted = true
''')
          replace(wall, '''                self.watch(url: url, scene: playable)
''', '''                self.watch(url: url, scene: sourceScene)
''')
          replace(wall, '''                if !reloading && !transient { self.onSelectionCommitted?(url) }
''', '''                if !reloading && !transient {
                    self.onSelectionCommitted?(url)
                    self.onSelectionCommittedVariant?(url, self.activeVariantID)
                }
''')
          replace(wall, '''            self.select(url, reloading: true)
''', '''            self.select(url, variantID: self.activeVariantID, reloading: true)
''')
          replace(wall, '''    var statusDescription: String {
        let title = playable?.title ?? selectedURL?.deletingPathExtension().lastPathComponent ?? "No wallpaper selected"
''', '''    var activeVariantName: String? {
        activeVariantID.flatMap { id in sourceScene?.variants.first(where: { $0.id == id })?.name }
    }
    var isActiveSceneModified: Bool { !runtimeControlValues.isEmpty }
    var activeDisplayName: String {
        let title = sourceScene?.title ?? playable?.title ?? selectedURL?.deletingPathExtension().lastPathComponent ?? "No wallpaper selected"
        var parts = [title]
        if let activeVariantName { parts.append(activeVariantName) }
        if isActiveSceneModified { parts.append("Modified") }
        return parts.joined(separator: " · ")
    }
    var statusDescription: String {
        let title = activeDisplayName
''')
          replace(wall, '''    var activeScene: SceneDescriptor? { playable }
''', '''    var activeScene: SceneDescriptor? { playable }
    var activeSourceScene: SceneDescriptor? { sourceScene }
''')
          replace(wall, '''        if let lastReloadError { menu.addItem(withTitle: "Edit not applied: " + lastReloadError, action: nil, keyEquivalent: "") }
        if let selectedURL { menu.addItem(withTitle: selectedURL.lastPathComponent, action: nil, keyEquivalent: "") }
''', '''        if let lastReloadError { menu.addItem(withTitle: "Edit not applied: " + lastReloadError, action: nil, keyEquivalent: "") }
        if let variantWarning { menu.addItem(withTitle: variantWarning, action: nil, keyEquivalent: "") }
        if selectedURL != nil { menu.addItem(withTitle: activeDisplayName, action: nil, keyEquivalent: "") }
''')
          replace(wall, '''    func applySceneParameters(_ parameters: [String: SceneParameter]) throws {
        guard let original = playable, !isLoading,
              Set(parameters.keys) == Set(original.parameters.keys),
              parameters.values.allSatisfy(\\.isValid) else {
            throw SceneError.invalid("The active scene controls changed. Select the wallpaper again and retry.")
        }
        var next = original
        next.parameters = parameters
        _ = try next.evaluated()
        for surface in surfaces {
            guard surface.updateScene(next) else {
                surfaces.forEach { _ = $0.updateScene(original) }
                throw SceneError.invalid("The scene controls could not be applied. The previous values were restored.")
            }
        }
        playable = next
        updateMenu()
    }
''', '''    func applySceneParameters(_ parameters: [String: SceneParameter]) throws {
        guard let sourceScene, let original = playable, let selectedURL, !isLoading else {
            throw SceneError.invalid("The active scene controls changed. Select the wallpaper again and retry.")
        }
        let base = sourceScene.applyingVariant(id: activeVariantID).scene
        guard Set(parameters.keys) == Set(base.parameters.keys), parameters.values.allSatisfy(\\.isValid) else {
            throw SceneError.invalid("The active scene controls changed. Select the wallpaper again and retry.")
        }
        var runtime: [String: SceneControlValue] = [:]
        for key in parameters.keys {
            guard let proposed = parameters[key], let inherited = base.parameters[key] else { continue }
            let value = SceneControlValue(proposed)
            if value != SceneControlValue(inherited) { runtime[key] = value }
        }
        let effective = compose(source: sourceScene, variantID: activeVariantID, runtimeValues: runtime)
        try updateLiveScene(effective.scene, rollingBackTo: original)
        playable = effective.scene
        runtimeControlValues = effective.runtimeValues
        syncSystemBackdrop(scene: effective.scene, sourceURL: selectedURL, request: generation)
        updateMenu()
    }
''')
          replace(wall, '''            resumeDefaults.removeObject(forKey: Self.pauseKey)
''', '''            resumeDefaults.removeObject(forKey: Self.pauseKey)
            resumeDefaults.removeObject(forKey: Self.resumeVariantKey)
''', 1)
          replace(wall, '''        selectedURL = nil
        playable = nil
''', '''        selectedURL = nil
        playable = nil
        sourceScene = nil
        activeVariantID = nil
        unavailableVariantID = nil
        runtimeControlValues = [:]
        variantWarning = nil
''')

          # ---- Library selected-detail variant state; grid thumbnails stay canonical ----
          lib = "Sources/Harness/SceneLibraryController.swift"
          replace(lib, '''    private let poster = NSImageView()
''', '''    private let poster = NSImageView()
    private let variantPicker = NSPopUpButton()
''')
          replace(lib, '''    private var selected: Item? {
        didSet { UserDefaults.standard.set(selected?.id, forKey: "Idlesse.library.selectedID") }
    }
''', '''    private var selected: Item? {
        didSet {
            if oldValue?.id != selected?.id { selectedVariantID = nil; variantPicker.isHidden = true }
            UserDefaults.standard.set(selected?.id, forKey: "Idlesse.library.selectedID")
        }
    }
    private var selectedVariantID: UUID?
''')
          replace(lib, '''    struct DesktopState {
        let url: URL?
        let paused: Bool
        let canPause: Bool
        let scene: SceneDescriptor?
    }
''', '''    struct DesktopState {
        let url: URL?
        let paused: Bool
        let canPause: Bool
        let scene: SceneDescriptor?
        let variantID: UUID?
        let modified: Bool
    }
''')
          replace(lib, '''    private var desktopScene: SceneDescriptor?
''', '''    private var desktopScene: SceneDescriptor?
    private var desktopVariantID: UUID?
    private var desktopModified = false
''')
          replace(lib, '''    private var onUse: (URL) -> Void
    private var onEdit: (URL, Bool) -> Void
''', '''    private var onUse: (URL) -> Void
    private var onEdit: (URL, Bool) -> Void
    var onUseVariant: ((URL, UUID?) -> Void)?
    var onEditVariant: ((URL, Bool, UUID?) -> Void)?
''')
          replace(lib, '''            onUse(opened.url)
''', '''            if let onUseVariant { onUseVariant(opened.url, collection.selection(for: item.id)?.variantID) }
            else { onUse(opened.url) }
''', 1)
          replace(lib, '''        favorite.target = self; favorite.action = #selector(toggleFavorite)
        apply.target = self; apply.action = #selector(useScene)
''', '''        favorite.target = self; favorite.action = #selector(toggleFavorite)
        variantPicker.target = self; variantPicker.action = #selector(variantChanged)
        variantPicker.setAccessibilityLabel("Scene variant")
        variantPicker.isHidden = true
        apply.target = self; apply.action = #selector(useScene)
''')
          replace(lib, '''        let primary = NSStackView(views: [apply, edit, more, clearSearchButton])
''', '''        let primary = NSStackView(views: [variantPicker, apply, edit, more, clearSearchButton])
''')
          replace(lib, '''        desktopScene = state.scene
        activeID = state.url.flatMap { itemID(for: $0) }
''', '''        desktopScene = state.scene
        desktopVariantID = state.variantID
        desktopModified = state.modified
        activeID = state.url.flatMap { itemID(for: $0) }
''')
          replace(lib, '''        desktopActions.isHidden = !activeSelected
        pauseDesktop.title = desktopPaused ? "Resume" : "Pause"
''', '''        desktopActions.isHidden = !activeSelected
        if activeSelected {
            let variantName = desktopVariantID.flatMap { id in desktopScene?.variants.first(where: { $0.id == id })?.name }
            activeDesktopLabel.stringValue = "● On Desktop" + (variantName.map { " · " + $0 } ?? "") + (desktopModified ? " · Modified" : "")
        }
        pauseDesktop.title = desktopPaused ? "Resume" : "Pause"
''')
          insert_before(lib, '''    private func preview() {
''', '''    private func variantCacheKey(itemID: String, variantID: UUID?) -> String {
        itemID + "|" + (variantID?.uuidString ?? "default")
    }
    private func configureVariantPicker(scene: SceneDescriptor) {
        variantPicker.removeAllItems()
        variantPicker.addItem(withTitle: "Default")
        for variant in scene.variants {
            variantPicker.addItem(withTitle: variant.name)
            variantPicker.lastItem?.representedObject = variant.id.uuidString
        }
        if let selectedVariantID, let index = scene.variants.firstIndex(where: { $0.id == selectedVariantID }) {
            variantPicker.selectItem(at: index + 1)
        } else {
            selectedVariantID = nil
            variantPicker.selectItem(at: 0)
        }
        variantPicker.isHidden = scene.variants.isEmpty
    }
    @objc private func variantChanged() {
        if let raw = variantPicker.selectedItem?.representedObject as? String { selectedVariantID = UUID(uuidString: raw) }
        else { selectedVariantID = nil }
        preview()
    }

''')
          replace(lib, '''    @objc private func refreshPreview() {
        if let selected { cache.removeValue(forKey: selected.id); cacheOrder.removeAll { $0 == selected.id } }
        preview()
    }
''', '''    @objc private func refreshPreview() {
        if let selected {
            let prefix = selected.id + "|"
            for key in cache.keys where key.hasPrefix(prefix) { cache.removeValue(forKey: key) }
            cacheOrder.removeAll { $0.hasPrefix(prefix) }
        }
        preview()
    }
''')
          replace(lib, '''        poster.image = nil
        favorite.isEnabled = selected != nil
''', '''        poster.image = nil
        if selected == nil { variantPicker.isHidden = true }
        favorite.isEnabled = selected != nil
''')
          replace(lib, '''                let revision = try await Task.detached(priority: .utility) { try PosterRevision.read(url) }.value
                try Task.checkCancellation()
                guard token == self.generation else { return }
                if let cached = self.cache[selected.id], cached.revision == revision {
                    self.poster.image = cached.image
                    self.detail.stringValue = cached.note
                    return
                }
                self.cache.removeValue(forKey: selected.id)
                let scene = try await LocalSceneSource().resolve(url)
                try Task.checkCancellation()
                guard token == self.generation else { return }
                let image: NSImage
                let previewTime = scene.metadata?.previewTime ?? 2
                let sourceDetails = try await Self.sourceDetails(url)
                let note = sourceDetails.isEmpty ? (scene.animated ? "Animated scene" : "Scene") : String(sourceDetails.dropFirst(3))
                let clock = SceneClock(now: { 0 })
                try clock.configure(timeline: scene.timeline)
                try clock.seek(to: previewTime)
                let renderer = try MetalSceneRenderer(playable: scene, bounds: NSRect(x: 0, y: 0, width: 1024, height: 576), scale: 1, clock: clock, onError: { _ in })
''', '''                let revision = try await Task.detached(priority: .utility) { try PosterRevision.read(url) }.value
                try Task.checkCancellation()
                guard token == self.generation else { return }
                let scene = try await LocalSceneSource().resolve(url)
                try Task.checkCancellation()
                guard token == self.generation else { return }
                self.configureVariantPicker(scene: scene)
                let application = scene.applyingVariant(id: self.selectedVariantID)
                self.selectedVariantID = application.selectedVariantID
                let effectiveScene = application.scene
                let cacheKey = self.variantCacheKey(itemID: selected.id, variantID: self.selectedVariantID)
                if let cached = self.cache[cacheKey], cached.revision == revision {
                    self.poster.image = cached.image
                    self.detail.stringValue = cached.note
                    return
                }
                self.cache.removeValue(forKey: cacheKey)
                let image: NSImage
                let previewTime = effectiveScene.metadata?.previewTime ?? 2
                let sourceDetails = try await Self.sourceDetails(url)
                let baseNote = sourceDetails.isEmpty ? (effectiveScene.animated ? "Animated scene" : "Scene") : String(sourceDetails.dropFirst(3))
                let note = baseNote + (self.selectedVariantID.flatMap { id in scene.variants.first(where: { $0.id == id })?.name }.map { " · " + $0 } ?? "")
                let clock = SceneClock(now: { 0 })
                try clock.configure(timeline: effectiveScene.timeline)
                try clock.seek(to: previewTime)
                let renderer = try MetalSceneRenderer(playable: effectiveScene, bounds: NSRect(x: 0, y: 0, width: 1024, height: 576), scale: 1, clock: clock, onError: { _ in })
''')
          replace(lib, '''                try await renderer.prepareOfflineVideo(at: scene.timeline?.videosFollowScene == true ? clock.time : previewTime,
''', '''                try await renderer.prepareOfflineVideo(at: effectiveScene.timeline?.videosFollowScene == true ? clock.time : previewTime,
''')
          replace(lib, '''                self.cacheOrder.removeAll { $0 == selected.id }
                while self.cacheOrder.count >= 4 { self.cache.removeValue(forKey: self.cacheOrder.removeFirst()) }
                self.cacheOrder.append(selected.id)
                self.cache[selected.id] = (image, note, revision)
''', '''                self.cacheOrder.removeAll { $0 == cacheKey }
                while self.cacheOrder.count >= 4 { self.cache.removeValue(forKey: self.cacheOrder.removeFirst()) }
                self.cacheOrder.append(cacheKey)
                self.cache[cacheKey] = (image, note, revision)
''')
          replace(lib, '''            do { try store.toggleMembership(sceneID: selected.id, collectionID: id); reload() }
''', '''            do {
                try store.toggleMembership(selection: .init(sceneID: selected.id, variantID: selectedVariantID), collectionID: id)
                reload()
            }
''')
          replace(lib, '''            if editing {
                retainEditAccess(opened.access)
                onEdit(opened.url, asCopy || selected.builtin != nil)
            } else {
                stopRotation()
                retainUseAccess(opened.access)
                onUse(opened.url)
                if !embedded { window?.orderOut(nil) }
            }
''', '''            if editing {
                retainEditAccess(opened.access)
                if let onEditVariant { onEditVariant(opened.url, asCopy || selected.builtin != nil, selectedVariantID) }
                else { onEdit(opened.url, asCopy || selected.builtin != nil) }
            } else {
                stopRotation()
                retainUseAccess(opened.access)
                if let onUseVariant { onUseVariant(opened.url, selectedVariantID) }
                else { onUse(opened.url) }
                if !embedded { window?.orderOut(nil) }
            }
''')

          # ---- Main host wiring consumes Library's explicit chosen variant ----
          main = "Sources/Harness/main.swift"
          replace(main, '''        library?.onPeek = { [weak self] url in self?.wallpaper.peek(url) }
''', '''        library?.onUseVariant = { [weak self] url, variantID in
            self?.wallpaper.select(url, variantID: variantID, automatic: true)
        }
        library?.onPeek = { [weak self] url in self?.wallpaper.peek(url) }
''')
          replace(main, '''            guard let self else { return .init(url: nil, paused: false, canPause: false, scene: nil) }
            return .init(url: self.wallpaper.selectedURL,
                         paused: self.wallpaper.pausedByUser,
                         canPause: self.wallpaper.canPauseActiveScene,
                         scene: self.wallpaper.activeScene)
''', '''            guard let self else { return .init(url: nil, paused: false, canPause: false, scene: nil, variantID: nil, modified: false) }
            return .init(url: self.wallpaper.selectedURL,
                         paused: self.wallpaper.pausedByUser,
                         canPause: self.wallpaper.canPauseActiveScene,
                         scene: self.wallpaper.activeScene,
                         variantID: self.wallpaper.activeVariantID,
                         modified: self.wallpaper.isActiveSceneModified)
''')

          # ---- Foundation regression coverage for collection migration/pinning ----
          tests = "Tests/LibraryTests.swift"
          replace(tests, '''    struct LegacyCatalog: Codable {
        var entries: [LegacyEntry]
        var favorites: Set<String>
        var recent: [String: Date]
        var collections: [SceneLibraryStore.Collection]?
    }
''', '''    struct LegacyCollection: Codable {
        var id: String
        var name: String
        var sceneIDs: [String]
        var playback: SceneLibraryStore.Playback?
    }
    struct LegacyCatalog: Codable {
        var entries: [LegacyEntry]
        var favorites: Set<String>
        var recent: [String: Date]
        var collections: [LegacyCollection]?
    }
''')
          replace(tests, '''        try store.toggleMembership(sceneID: "builtin.Undertow", collectionID: collection.id)
''', '''        try store.toggleMembership(sceneID: "builtin.Undertow", collectionID: collection.id)
        let midnightID = UUID()
        try store.toggleMembership(selection: .init(sceneID: "builtin.Undertow", variantID: midnightID), collectionID: collection.id)
        precondition(store.catalog.collections.first?.selection(for: "builtin.Undertow")?.variantID == midnightID)
''', 1)
          replace(tests, '''        precondition(reopened.catalog.collections.first?.sceneIDs == [entry.id, "builtin.Undertow"])
''', '''        precondition(reopened.catalog.collections.first?.sceneIDs == [entry.id, "builtin.Undertow"])
        precondition(reopened.catalog.collections.first?.selection(for: "builtin.Undertow")?.variantID == midnightID)
''', 1)
          replace(tests, '''        let legacyCollection = SceneLibraryStore.Collection(id: "legacy-collection", name: "Legacy Collection",
            sceneIDs: [legacyID, "builtin.Undertow"], playback: .init(minutes: 30, shuffle: false))
''', '''        let legacyCollection = LegacyCollection(id: "legacy-collection", name: "Legacy Collection",
            sceneIDs: [legacyID, "builtin.Undertow"], playback: .init(minutes: 30, shuffle: false))
''')
          replace(tests, '''        precondition(legacyStore.catalog.collections.first?.sceneIDs == [legacyID, "builtin.Undertow"])
''', '''        precondition(legacyStore.catalog.collections.first?.sceneIDs == [legacyID, "builtin.Undertow"])
        precondition(legacyStore.catalog.collections.first?.selections.allSatisfy { $0.variantID == nil } == true,
                     "Legacy scene IDs must decode as Default variant selections")
''', 1)

          # ---- Document the composed runtime semantics implemented here ----
          docs = Path("docs/scene-variants.md")
          docs.write_text(docs.read_text() + '''

## Product integration

Library selected-detail previews may choose an authored variant, while ordinary grid/list thumbnails stay on Default. Collections persist `SceneSelection(sceneID, variantID)`; legacy `sceneIDs` decode as Default.

Wallpaper playback keeps three layers separate: the resolved package scene, the selected authored variant, and session-only control changes. Switching a variant clears the temporary control layer and updates live surfaces in place, preserving scene time and media phase. The selected variant UUID is saved beside the wallpaper bookmark, while ad-hoc control edits remain session-only. The menu/status title appends the variant name and `Modified` when temporary controls differ from that variant. System backdrop stills are regenerated from the same effective scene.
''')
          PY
          git diff --check
      - name: Run focused and full checks
        run: |
          ./test.sh
      - name: Commit, remove helper, and push
        run: |
          git config user.name "Palette"
          git config user.email "actions@users.noreply.github.com"
          rm .github/workflows/palette-32-host-library.yml
          git add -A
          git diff --cached --check
          git commit -m "Finish #32 wallpaper and Library variant selection"
          git push origin HEAD:palette/32-variants-host-library
